apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: readarr-ingress
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: readarr.nverk.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: readarr-service
                port:
                  number: 8787
